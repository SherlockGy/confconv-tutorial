[package]
name = "confconv"
version = "0.1.0"
edition = "2021"
description = "配置文件格式转换工具 - 第1章版本"

[dependencies]
# clap: Rust 最流行的命令行参数解析库
# derive feature: 启用 #[derive(Parser)] 宏
clap = { version = "4.5", features = ["derive"] }
